- except ||= []

.sidebar-social.row
  .sidebar-widget
    = link_to "Comments", "#disqus_thread", 'data-disqus-identifier' => disqus_identifier(postable)

  - if not except.include? :print
    .sidebar-widget
      = link_to "Print", print_site_article_path(postable)

  - if not except.include? :email
    .sidebar-widget
      = link_to "Email", mailto_article(postable)

  .sidebar-widget= facebook_recommend(permanent_post_url(postable))
  .sidebar-widget= twitter_share(permanent_post_url(postable))
  .sidebar-widget= google_plusone(permanent_post_url(postable))
