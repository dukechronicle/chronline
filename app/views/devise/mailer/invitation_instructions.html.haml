%p Hello #{@resource.email}!

%p Someone has invited you to #{site_root_url(subdomain: :www)}, you can accept it through the link below.

%p= link_to 'Accept invitation', accept_invitation_url(@resource, invitation_token: @resource.invitation_token, subdomain: :admin)

%p
  If you don't want to accept the invitation, please ignore this email.
  %br
  Your account won't be created until you access the link above and set your password.
