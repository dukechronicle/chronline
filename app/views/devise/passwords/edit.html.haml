- provide :title, "Change your password"

.span-8
  .rounded
    .form-container
      = render 'beta/partials/flash', flash: flash
      = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
        = f.error_notification

        = f.input :reset_password_token, as: :hidden
        = f.full_error :reset_password_token

        %fieldset
          .form_inputs
            = f.input :password, label: "New password", required: true
            = f.input :password_confirmation, label: "Confirm your new password", required: true

          .form-actions
            = f.button :submit, "Change my password", class: 'button button-rounded button-primary'

      = render 'devise/shared/links'

.span-4.last
  = render partial: 'beta/partials/advertisement', locals: {zone: :medium_rectangle}
