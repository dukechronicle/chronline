.story-list
  %h2.vertical-label= title
  .rounded
    .content-container
      - articles.each do |article|
        %a.list-story.hover.highlight-right{href: site_article_path(article)}
          %h3.title= article.title
