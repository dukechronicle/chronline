%h3.title= article.title
= render partial: 'site/articles/subtitle', locals: {article: article, notime: options[:notime]}
%p.subtitle
  %span.author= byline(article)
  %span.date= display_date(article)
