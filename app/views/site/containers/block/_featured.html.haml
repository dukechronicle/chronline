.block.block-featured
  .rounded
    %a.hover.highlight-bottom{href: site_article_path(article)}
      - if article.image
        .image-container
          = image_tag article.image.original.url(:large_rect)
      .content-container
        %h3.title= article.title
        %p.teaser= article.teaser
