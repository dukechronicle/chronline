- image = gallery.get_gallery_images.first 
- if image
  %a{href: site_gallery_path(image.get_gallery.gid) }
    %img{:src => image.url(300), :data => {:link => image.photoshelter_url}, :height => 200, width: 200}
