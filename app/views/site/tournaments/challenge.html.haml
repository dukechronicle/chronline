- provide :title, "#{@tournament.full_name} Challenge"

.clearfix
  .span-8
    .rounded
      .article-container
        .article-body!= @tournament.challenge_text

  .span-4.last
    = render partial: 'site/partials/advertisement', locals: {zone: :medium_rectangle}
